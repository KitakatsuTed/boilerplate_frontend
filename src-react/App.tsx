/**
 * ルートコンポーネント（React版）
 *
 * 注: React Routerを使用しているため、このファイルは現在使用されていません。
 * main.tsxでRouterProviderを直接使用しています。
 *
 * 必要に応じてレイアウトコンポーネントとして使用できます。
 */

export default function App() {
  return <div>App</div>
}
