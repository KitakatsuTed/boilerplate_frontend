/**
 * OpenAPI型定義（自動生成用プレースホルダー）
 *
 * このファイルは `npm run generate:api-types` で自動生成されます。
 * バックエンドのOpenAPIスキーマから型定義を生成します。
 *
 * 使用方法:
 * ```bash
 * npm run generate:api-types
 * ```
 *
 * 生成コマンド:
 * ```bash
 * npx openapi-typescript http://localhost:8000/openapi.json -o src/api/types/api.ts
 * ```
 */

export type paths = Record<string, any>
export type components = Record<string, any>
